{
  "name": "repo-gc-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.3",
  "license": "MIT",
  "repository": "https://github.com/repo-gc/repo-gc",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "test": "pnpm -r --filter './packages/*' test",
    "typecheck": "pnpm -r --filter './packages/*' typecheck",
    "publish:all": "pnpm build && pnpm test && pnpm -r --filter repo-gc-shared publish --access public && pnpm -r --filter repo-gc-typescript publish --access public && pnpm -r --filter repo-gc publish --access public && pnpm -r --filter repo-gc-rust publish --access public"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "unrs-resolver"]
  }
}
